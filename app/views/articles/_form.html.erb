<%# This is a shared view called partial %>
<%# all occurrences of "@article" have been replaced with "article" 
Because partials are shared code, it is best practice that they do
not depend on specific instance variables set by a controller action %>

<%# Note: Instead article will be passed as local variable %>

<%= form_with model: article do |form| %>
<%# full_messages_for method returns an array of user-friendly error messages
for specified attr. unless no errors then empty %>
    <div>
        <%= form.label :title %><br>
        <%= form.text_field :title %>
        <% article.errors.full_messages_for(:title).each do |message|%>
            <div><%= message %></div>
        <% end %>
    </div>
    <div>
        <%= form.label :body %><br>
        <%= form.text_area :body %>
        <% article.errors.full_messages_for(:body).each do |message|%>
            <div><%= message %></div>
        <% end %>
    </div>

    <div>
        <%= form.submit %>
    </div>
<% end %>

<%# A partial's filename must be prefixed with an underscore, e.g. _from.html.erb.
But when rendering, it is referenced without the underscore, e.g render "from". %>